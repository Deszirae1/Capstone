import React, { useState } from 'react';
import { useParams } from 'react-router-dom';

const ReviewForm = ({ businesses, auth })=> {
    return (
      <h1>Placeholder for Businesses { businesses.length }</h1>
    );
  }

  export default ReviewForm; 

 